political_decisions = {
	kill_bruhgundy = {
		potential = {
			tag = FRA
			NOT = { has_country_flag = eat_bruh }
        }
		allow = {
			BRG = {
				vassal_of = FRA
			}
		}
		effect = {
			set_country_flag = eat_bruh
			prestige = 5
			BRG = { annex_to = THIS }
            any_owned = {
            	limit = { is_core = BRG }
            	add_core = FRA 
			}
			BRG = { all_core = { remove_core = BRG } }
		}
	}

    french_nationalism = {
        potential = {
			tag = FRA
			NOT = { has_country_flag = franch_nat }
        }
        allow = {
            OR = {
				revolution_n_counterrevolution = 1
				GER = { exists = yes }
			}
        }
    	effect = {
    		set_country_flag = franch_nat
        	add_accepted_culture = breton
        	add_accepted_culture = occitan
        	add_accepted_culture = gascon
			add_accepted_culture = burgundian
		}
    }
	french_nv_change = {
		potential = {
			tag = FRA
			NOT = { has_country_flag = french_nv_change }
		}
		allow = {
			OR = {
				GER = { exists = yes }
				year = 1865
			}
		}
		effect = {
			nationalvalue = nv_equality
			set_country_flag = french_nv_change
		}
	}
}